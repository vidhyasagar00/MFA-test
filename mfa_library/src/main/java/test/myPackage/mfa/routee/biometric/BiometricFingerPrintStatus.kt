package test.myPackage.mfa.routee.biometric

/**
 * It was enum which returns to our app with status of Biometric authorization
 *
 */
enum class BiometricFingerPrintStatus {
    SUCCESS,
    CANCELED,
    FAILED
}